#server:
#  port: 8500

spring:
  application:
    name: nacos-server-config
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
#        file-extension: yaml
